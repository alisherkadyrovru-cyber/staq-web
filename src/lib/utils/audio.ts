/**
 * Audio session configuration.
 * Real implementation uses expo-av — requires a custom dev build (not Expo Go).
 * This is a no-op stub for development in Expo Go.
 */
export async function configureAudioSession(): Promise<void> {
  // No-op in Expo Go. Will be implemented with expo-av in the native build.
}
